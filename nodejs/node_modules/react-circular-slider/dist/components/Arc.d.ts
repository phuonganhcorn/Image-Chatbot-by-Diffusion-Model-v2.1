/// <reference types="react" />
import * as React from 'react';
export interface ArcProps {
    startAngle?: number;
    endAngle?: number;
    padding: number;
    radius: number;
    thickness: number;
    style?: React.CSSProperties;
    mask?: JSX.Element;
    [index: string]: any;
}
declare const _default: ({radius, startAngle, endAngle, padding, thickness, style, children, mask, ...otherProps}: ArcProps) => JSX.Element;
export default _default;
